{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && (git diff --cached --quiet || git commit -m 'auto: deploy changes') && git push 2>&1 || true",
            "shell": "bash",
            "timeout": 60,
            "statusMessage": "Déploiement automatique en cours..."
          }
        ]
      }
    ]
  }
}
